execute_python_on_issue_update:
  stage: execute-script
  script:
    - python3 /home/gitlab-runner/scripts-secures/script.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
  variables:
    GITHUB_TOKEN: $GITHUB_TOKEN
